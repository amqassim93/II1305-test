{{title}}
{{#unless isListEnabled}}
<ul>
    <li>Home</li>
    <li>About</li>
    <li>Donate</li>
    
</ul>
{{/unless}}

{{#with author as |x|}} <!-- Go into object authors view and can directly referens its sub objects --->

    <h2> My name is {{x.firstName}} {{x.lastName}}</h2>
    <p>My first project is {{x.project.name}}</p>

{{/with}}
